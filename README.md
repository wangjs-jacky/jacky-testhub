# 📋 Table Utils Extension

<div align="center">

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Chrome](https://img.shields.io/badge/Chrome-114+-brightgreen.svg)
![Plasmo](https://img.shields.io/badge/Plasmo-0.88.0-purple.svg)

**一个强大的 Chrome 扩展，用于快速操作和管理网页表格数据**

[功能特性](#-功能特性) • [快速开始](#-快速开始) • [使用说明](#-使用说明) • [开发指南](#-开发指南)

</div>

---

## ✨ 功能特性

- 🔍 **检查表格行数** - 快速查看表格总行数、有内容的行数、空行数
- ➕ **自动添加行** - 自动点击"添加步骤"按钮，直到达到目标行数
- 📥 **提取表格数据** - 从表格中提取步骤描述和预期结果，支持 JSON 格式导出
- 📤 **填充表格数据** - 将编辑后的数据批量填充回表格
- ⌨️ **快捷键支持** - 使用 `Ctrl+U` / `Command+U` 快速打开/关闭侧边栏
- 🎨 **侧边栏界面** - 基于 Chrome Side Panel API，提供更大的操作空间

## 🚀 快速开始

### 安装方式

#### 方式一：从源码构建（推荐）

1. **克隆仓库**

   ```bash
   git clone https://github.com/your-username/jacky-testhub.git
   cd jacky-testhub
   ```
2. **安装依赖**

   ```bash
   npm install
   ```
3. **构建扩展**

   ```bash
   npm run build
   ```
4. **加载到 Chrome**

   - 打开 Chrome 浏览器，访问 `chrome://extensions/`
   - 开启"开发者模式"（右上角开关）
   - 点击"加载已解压的扩展程序"
   - 选择 `build/chrome-mv3-prod` 目录

#### 方式二：使用控制台脚本

如果你只需要在控制台使用，可以直接使用 `table-utils.js`：

1. 打开包含表格的网页
2. 按 `F12` 打开开发者工具
3. 复制 `table-utils.js` 的全部内容到控制台执行
4. 使用 `window.tableUtils` 对象的方法

## 📖 使用说明

### 打开侧边栏

- **点击扩展图标**：点击浏览器工具栏中的扩展图标
- **快捷键**：使用 `Ctrl+U` (Windows/Linux) 或 `Command+U` (macOS)

> 💡 可以在 `chrome://extensions/shortcuts` 中自定义快捷键

### 功能操作

#### 1. 检查行数

点击"🔍 检查行数"按钮，查看当前表格的行数统计：

- 总行数
- 有内容的行数
- 空行数

#### 2. 添加行

点击"➕ 添加行"按钮，输入目标行数，自动添加行直到达到目标数量。

#### 3. 提取数据

点击"📥 提取数据"按钮，从表格中提取数据并显示在文本框中，支持编辑。

#### 4. 填充数据

编辑文本框中的数据后，点击"📤 填充数据"按钮，将数据填充回表格。

### 控制台 API

如果你使用控制台脚本，可以使用以下 API：

```javascript
// 检查表格行数
tableUtils.checkTableRows()

// 获取当前行数
tableUtils.getCurrentRowCount()

// 添加行直到达到目标数量
await tableUtils.addTableRows(13)

// 提取表格数据
const data = tableUtils.extractTableData()

// 填充表格数据
tableUtils.fillTableData(data)

// 导出为 JSON
const json = tableUtils.exportToJSON(data)
```

### 数据格式

提取的数据格式为 JSON 数组：

```json
[
  {
    "步骤序号": 1,
    "步骤描述": "等待\"10000ms\"",
    "预期结果": ""
  },
  {
    "步骤序号": 2,
    "步骤描述": "点击[SKU区域]下的[套餐卡片2]",
    "预期结果": "**APP** [底部bar]展示\"低至\",\"CNY 3.00\",\"每人\""
  }
]
```

## 🛠️ 开发指南

### 项目结构

```
.
├── sidepanel.tsx          # 侧边栏 UI 组件 (React)
├── background.ts           # 背景脚本 - 处理快捷键和侧边栏逻辑
├── popup.css               # 样式文件
├── contents/
│   └── inject.ts          # Content Script - 注入到页面的脚本
├── table-utils.js         # 全局工具脚本 (可在控制台使用)
├── assets/                # 图标资源
├── package.json
├── tsconfig.json
└── README.md
```

### 开发命令

```bash
# 安装依赖
npm install

# 开发模式（热重载）
npm run dev

# 构建生产版本
npm run build

# 打包扩展
npm run package
```

### 技术栈

- **[Plasmo](https://www.plasmo.com/)** - 现代浏览器扩展框架
- **[React](https://react.dev/)** - UI 框架
- **[TypeScript](https://www.typescriptlang.org/)** - 类型安全
- **Chrome Extension Manifest V3** - 最新扩展规范

### 浏览器兼容性

- ✅ Chrome 114+
- ✅ Edge 114+
- ❌ Firefox（暂不支持 Side Panel API）

## 📝 注意事项

1. **表格结构要求**

   - 支持标准 HTML 表格 (`<table>`)
   - 支持 Ant Design 表格 (`.ant-table-tbody`)
   - 表格需要包含"步骤描述"和"预期结果"列
2. **动态内容**

   - 如果表格内容是动态加载的，请确保在内容加载完成后再执行操作
3. **React 应用**

   - 填充数据时会触发相应的事件，确保 React 状态更新

## 🤝 贡献

欢迎贡献！如果你有任何建议或发现问题，请：

1. Fork 本仓库
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启一个 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 🙏 致谢

- [Plasmo](https://www.plasmo.com/) - 优秀的浏览器扩展开发框架
- [Chrome Extensions API](https://developer.chrome.com/docs/extensions/) - Chrome 扩展 API 文档

## 📧 联系方式

如有问题或建议，请通过以下方式联系：

- 提交 [Issue](https://github.com/your-username/jacky-testhub/issues)
- 创建 [Pull Request](https://github.com/your-username/jacky-testhub/pulls)

---

<div align="center">

**如果这个项目对你有帮助，请给个 ⭐ Star！**

Made with ❤️ by Jacky

</div>
